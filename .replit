run = "npm start"
entrypoint = "server/index.js"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "npm start"]
deploymentTarget = "cloudrun"

[env]
PORT = "3000"
NODE_ENV = "production"

[[ports]]
localPort = 3000
externalPort = 80
